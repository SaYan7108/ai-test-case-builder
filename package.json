{
  "name": "ai-test-case-builder",
  "version": "1.0.0",
  "description": "AI-generated test cases for saucedemo.com login, automated with Playwright",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
